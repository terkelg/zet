{
  "name": "zet",
  "version": "1.0.4",
  "repository": "terkelg/zet",
  "description": "Like Set() but with union, intersect, and difference built-in",
  "module": "dist/zet.mjs",
  "main": "dist/zet.js",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node builder",
    "pretest": "npm run build",
    "prepublishOnly": "npm run build",
    "test": "tape test/*.js | tap-spec"
  },
  "keywords": [
    "set",
    "sets",
    "intersect",
    "difference",
    "union"
  ],
  "devDependencies": {
    "mkdirplz": "1.0.0",
    "rewrite-imports": "^2.0.1",
    "tap-spec": "^5.0.0",
    "tape": "^4.10.1"
  }
}
